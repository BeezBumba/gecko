[package]
name = "dbglib"
version = "0.1.0"
edition = "2024"

[dependencies]
gecko = { path = "../gecko", default-features = false }
disasm = { path = "../disasm" }
image = { path = "../image", default-features = false }
egui = "0.33.3"
egui-phosphor = "0.11.0"
