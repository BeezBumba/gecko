[package]
name = "scripting"
version = "0.1.0"
edition = "2024"

[features]
hooks-mut-traps = ["gecko/hooks-mut-traps"]

[dependencies]
gecko = { path = "../gecko", features = ["hooks"] }
mlua = { version = "0.10.5", features = ["luau-jit", "send", "vendored"] }
tracing = "0.1.44"
